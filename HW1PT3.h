#pragma once
#include <algorithm>
#include <fstream>
#include <iostream>
#include <stdbool.h>
using namespace std;
const int arraySize = 100; int dataArray[arraySize];
void readFromArray(const string& fileName);